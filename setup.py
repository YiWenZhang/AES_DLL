"""
Setup script for AES_DLL Python package.

The pre-compiled AES_DLL.dll is bundled in the aesdll/ directory.
No compilation is needed during pip install — just package the DLL.
"""

from setuptools import setup

setup()
